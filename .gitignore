# Logs
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test artifacts
coverage
test-results
playwright-report

# Editor / tooling local config
.vscode/*
!.vscode/extensions.json
.idea
.claude/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


.env
# .env.production is intentionally committed: it holds only the PUBLIC production
# API URL (all VITE_* vars are baked into the client bundle — never a secret).
# Machine-specific overrides belong in *.local files, which are ignored above.

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
